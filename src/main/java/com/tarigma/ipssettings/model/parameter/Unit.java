package com.tarigma.ipssettings.model.parameter;

import com.tarigma.ipssettings.model.Emptyable;

public class Unit extends Emptyable {

    /**
     * Construct emptyable
     *
     * @param isEmpty is empty
     */
    public Unit(boolean isEmpty) {
        super(isEmpty);
    }
}
